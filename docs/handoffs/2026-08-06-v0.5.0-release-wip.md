# secdat v0.5.0 リリース WIP 引継ぎ

作成日: 2026-08-06

対象: [Issue #193: Prepare and publish secdat v0.5.0](https://github.com/mako10k/secdat/issues/193)

基点コミット: `12065fd Fix incremental provenance and legacy agent lock`

## 凍結状態

時間切れのため、v0.5.0 のリリース作業を実装・レビュー修正途中で凍結した。
この WIP は acceptance、最終レビュー、リリース、Issue close、再インストールの
完了を意味しない。

- v0.5.0 タグは未作成・未 push。
- GitHub Release v0.5.0 は未作成。直前の read-only preflight は 404 だった。
- `origin/main` は変更していない。
- ローカルの `/usr/local/bin/secdat` は基点 `12065fd` のインストールのまま。
- 作業内容は WIP ブランチだけに commit/push する。

## 実装済み WIP

- core、bindings、manpage、gettext の version を 0.5.0 に揃えた。
- libtool version-info を `1:0:1` にし、SONAME `libsecdat.so.0` と artifact
  `libsecdat.so.0.1.0` の ABI regression を追加した。
- language bindings と v0.5.0 release notes を source distribution に含めた。
- libsecdat relink、Git-less build ID、shared/static SDK、private pkg-config
  dependency、locale-independent ABI の regression を追加・強化した。
- Go に shared 既定と `secdat_static` build tag の pkg-config 契約を追加した。
- release workflow に main/tag SHA、annotated tag subject、static-only distcheck、
  GitHub Release one-create/readback を文書化した。
- 最終レビューで見つかった #209-#212 に対し、ambiguous push readback、origin 由来の
  GitHub repository identity、commit blob 由来の release notes、installed static C
  consumer 検証を追加した。

## Issue 状態

Issue #193-#213 はすべて OPEN。

- #193: v0.5.0 release 親 Issue
- #194-#208: 初回実装・レビューで見つかった release/ABI/package 契約
- #209: ambiguous push 後に ref readback が飛ぶ
- #210: GitHub Release 宛先が検証済み origin に束縛されない
- #211: release notes が tagged commit blob に束縛されない
- #212: static C consumer の `pkg-config --static` 契約欠落
- #213: `tests/install_regression.sh` の既存 `LC_ALL=` が shellcheck SC1007

#194-#212 の実装候補は working tree にあるが、受入れ完了までは close しない。
#213 は Issue 化と readback までで、修正未着手。

## 確認済み

最終レビュー #209-#212 の修正前に、以下は PASS 済み。

- `make check`
- `make distcheck DISTCHECK_CONFIGURE_FLAGS=--enable-fuse`
- `make distcheck DISTCHECK_CONFIGURE_FLAGS='--enable-fuse --disable-shared --enable-static'`
- shared SDK regression
- static-only source archive SDK/Go consumer regression
- build-ID clean/dirty transition regression
- release metadata regression
- 対象 shell scripts の shellcheck
- `codex review --uncommitted` は上記 build/check/distcheck の PASS を再確認し、
  #209-#212 を報告した。

#209-#212 の修正後に確認した結果:

- `bash tests/release_metadata_regression.sh "$PWD"`: PASS
- `shellcheck tests/release_metadata_regression.sh tests/install_regression.sh`:
  FAIL (`tests/install_regression.sh` の既存 SC1007、Issue #213)

## 未完了・既知リスク

- #209-#212 の修正後は focused runtime test、`make check`、両 distcheck、最終レビューを
  再実行していない。
- `tests/install_regression.sh` の新しい staged installed-consumer compile/run は未実行。
- release workflow の shell snippet は実環境 dry-run/read-only review が必要。
- #213 は `LC_ALL=''` のように意図を明示する最小修正候補だが、RCA/検証前なので未変更。
- 最終レビューで追加 finding が出た場合は、従来どおり Issue-first で扱う。
- Issue comment/close、tag、GitHub Release、local install/daemon handover は未実施。

## 再開順

1. WIP ブランチとこの文書の commit SHA、`git status --short --branch` を確認する。
2. Issue #193-#213 の state/body を GitHub から read back する。
3. `.codex/agents/rca-shortest-path-solver.toml` に従い #213 を確認し、SC1007 を修正する。
4. `bash tests/release_metadata_regression.sh "$PWD"`、
   `shellcheck tests/release_metadata_regression.sh tests/install_regression.sh`、
   installed-consumer focused test を実行する。
5. `make check` と shared/static の両 FUSE-enabled `make distcheck` を再実行する。
6. 一貫性・対称性・網羅性を含む最終 review を行い、finding は Issue-first で修正する。
7. WIP commit を amend/squash して non-WIP release commit にする。WIP のまま tag を打たない。
8. `origin/main` を push して exact SHA readback後、annotated `v0.5.0` tag を one push し、
   tag object/peeled commit を read back する。
9. frozen repository/commit/notes blob から GitHub Release を最大1回 create し、全 field を
   exact readbackする。曖昧結果を blind retry しない。
10. 検証 evidence を #194-#213 に comment/readbackして closeし、#193 を最後に closeする。
11. `/usr/local` へ install し、version、SONAME、pkg-config、agent handover/status を確認する。

## 変更範囲

`git show --stat` で全体を確認すること。主な範囲は次のとおり。

- `configure.ac`, `Makefile.am`, `src/Makefile.am`, `libsecdat.pc.in`
- `bindings/` の manifests、Go pkg-config split、binding documentation
- `docs/release-workflow.md`, `docs/release-notes/v0.5.0.md`, manpages
- `tests/abi_regression.sh`, `tests/libtool_relink_regression.sh`,
  `tests/build_id_regression.sh`, `tests/install_regression.sh`,
  `tests/release_metadata_regression.sh`, `tests/sdk_regression.sh`,
  `tests/redaction_regression.sh`

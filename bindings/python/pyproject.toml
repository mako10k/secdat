[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "secdat-sdk"
version = "0.4.1"
description = "Python ctypes binding for libsecdat"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["secdat_sdk"]
